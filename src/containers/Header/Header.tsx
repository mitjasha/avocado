import React from "react";
import "./Header.scss";

const Header: React.FC = () => {
  return (
    <header className="header container">
      <div>Header</div>
    </header>
  );
};

export default Header;
